const initialState = {
	filter: {},
	
  data: [],
  count: 0,
	page: 1,
	perPage: 20,
  totalPages: 0,
}

const DataReducer = (state = initialState, action) => {
  switch (action.type) {    
    default:
      return state
  }
}

export default DataReducer
